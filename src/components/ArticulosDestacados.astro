---
const articulos = [
    {
        id: 1,
        imagen: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=600&h=800&fit=crop",
        categoria: "ENTREVISTA",
        titulo: "El futuro del trabajo remoto en LATAM",
        descripcion:
            "Conversamos con líderes de la industria sobre cómo está evolucionando el trabajo remoto en la región.",
    },
    {
        id: 2,
        imagen: "https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=600&h=800&fit=crop",
        categoria: "PRODUCTO",
        titulo: "Diseño de productos sostenibles",
        descripcion:
            "Cómo las startups están redefiniendo el diseño de productos con un enfoque en sostenibilidad.",
    },
];
---

<section class="py-16 border-t border-border">
    <div class="container">
        <!-- Section header -->
        <div class="mb-8">
            <p
                class="text-xs font-medium uppercase tracking-widest text-muted-foreground mb-2"
            >
                ARTÍCULOS DESTACADOS
            </p>
        </div>

        <!-- Articles grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {
                articulos.map((articulo) => (
                    <a href={`/articulos/${articulo.id}`} class="group block">
                        <div class="aspect-[4/3] overflow-hidden rounded-sm mb-4">
                            <img
                                src={articulo.imagen}
                                alt={articulo.titulo}
                                class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
                            />
                        </div>
                        <p class="text-xs font-medium uppercase tracking-widest text-muted-foreground mb-2">
                            {articulo.categoria}
                        </p>
                        <h3 class="font-serif text-xl md:text-2xl font-normal mb-2 group-hover:underline">
                            {articulo.titulo}
                        </h3>
                        <p class="text-sm text-muted-foreground">
                            {articulo.descripcion}
                        </p>
                    </a>
                ))
            }
        </div>
    </div>
</section>
