---

---

<section class="newsletter">
  <div class="newsletter-container">
    <div class="newsletter-content">
      <div class="newsletter-text">
        <h2 class="newsletter-title">Recibe Press cada mes</h2>
        <p class="newsletter-description">
          Entrevistas, proyectos, eventos y recursos del ecosistema tech latinoamericano. Sin spam, solo contenido que vale la pena.
        </p>
      </div>
      <div class="newsletter-form-wrapper">
        <form class="newsletter-form" id="newsletter-form">
          <input
            type="email"
            placeholder="tu@email.com"
            required
            class="newsletter-input"
          />
          <button type="submit" class="newsletter-button">Suscribirme</button>
        </form>
        <div class="newsletter-success" id="newsletter-success">
          <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
            <polyline points="22,6 12,13 2,6"/>
          </svg>
          <span>Gracias por suscribirte. Te llegará la próxima edición.</span>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('newsletter-form') as HTMLFormElement;
  const success = document.getElementById('newsletter-success') as HTMLElement;

  form?.addEventListener('submit', (e) => {
    e.preventDefault();
    form.style.display = 'none';
    success.style.display = 'flex';
  });
</script>

<style>
  .newsletter {
    padding: 4rem 0;
    background: var(--primary);
  }

  .newsletter-container {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .newsletter-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    align-items: center;
  }

  .newsletter-text {
    flex: 1;
  }

  .newsletter-title {
    font-family: Georgia, serif;
    font-size: 1.875rem;
    font-weight: 700;
    color: var(--primary-foreground);
  }

  .newsletter-description {
    margin-top: 0.5rem;
    color: var(--primary-foreground);
    opacity: 0.85;
    line-height: 1.6;
  }

  .newsletter-form-wrapper {
    width: 100%;
  }

  .newsletter-form {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .newsletter-input {
    width: 100%;
    padding: 0.75rem 1rem;
    font-size: 1rem;
    border: none;
    border-radius: var(--radius);
    background: var(--background);
    color: var(--foreground);
  }

  .newsletter-input::placeholder {
    color: var(--muted-foreground);
  }

  .newsletter-button {
    padding: 0.75rem 1.5rem;
    font-size: 0.875rem;
    font-weight: 500;
    border: none;
    border-radius: var(--radius);
    background: var(--background);
    color: var(--foreground);
    cursor: pointer;
    transition: opacity 0.2s;
    white-space: nowrap;
  }

  .newsletter-button:hover {
    opacity: 0.9;
  }

  .newsletter-success {
    display: none;
    align-items: center;
    gap: 0.75rem;
    color: var(--primary-foreground);
  }

  .icon {
    width: 1.25rem;
    height: 1.25rem;
  }

  @media (min-width: 640px) {
    .newsletter-form {
      flex-direction: row;
    }

    .newsletter-input {
      width: 16rem;
    }
  }

  @media (min-width: 768px) {
    .newsletter-content {
      flex-direction: row;
      gap: 4rem;
    }

    .newsletter-form-wrapper {
      width: auto;
    }
  }
</style>
