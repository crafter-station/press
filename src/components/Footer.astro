---
const navLinks = [
    { label: "Ediciones", href: "/ediciones" },
    { label: "Sobre Nosotros", href: "/sobre-nosotros" },
    { label: "Contacto", href: "/contacto" },
];

const socialLinks = [
    { label: "Twitter", href: "https://twitter.com/crafterstation" },
    { label: "LinkedIn", href: "https://linkedin.com/company/crafterstation" },
    { label: "GitHub", href: "https://github.com/crafterstation" },
];
---

<footer class="py-12 border-t border-border">
    <div class="container">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-12">
            <!-- Brand -->
            <div class="md:col-span-2">
                <div class="flex items-center gap-2 mb-4">
                    <span class="font-serif text-xl font-bold">Press</span>
                    <a
                        href="https://crafterstation.com"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="text-[10px] font-medium uppercase tracking-widest text-muted-foreground hover:text-foreground transition-colors"
                    >
                        by CRAFTERSTATION
                    </a>
                </div>
                <p
                    class="text-sm text-muted-foreground max-w-sm leading-relaxed"
                >
                    Una revista digital sobre tecnología, diseño e innovación en
                    Latinoamérica. Hecha con cariño desde la comunidad, para la
                    comunidad.
                </p>
            </div>

            <!-- Navigation -->
            <div>
                <h4 class="text-xs font-medium uppercase tracking-widest mb-4">
                    Navegación
                </h4>
                <nav class="flex flex-col gap-2">
                    {
                        navLinks.map((link) => (
                            <a
                                href={link.href}
                                class="text-sm text-muted-foreground hover:text-foreground transition-colors"
                            >
                                {link.label}
                            </a>
                        ))
                    }
                </nav>
            </div>

            <!-- Social -->
            <div>
                <h4 class="text-xs font-medium uppercase tracking-widest mb-4">
                    Síguenos
                </h4>
                <nav class="flex flex-col gap-2">
                    {
                        socialLinks.map((link) => (
                            <a
                                href={link.href}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="text-sm text-muted-foreground hover:text-foreground transition-colors"
                            >
                                {link.label}
                            </a>
                        ))
                    }
                </nav>
            </div>
        </div>

        <!-- Bottom bar -->
        <div
            class="pt-8 border-t border-border flex flex-col md:flex-row justify-between items-center gap-4"
        >
            <p class="text-xs text-muted-foreground">
                © 2025 CrafterStation. Press es un proyecto open source.
            </p>
            <div class="flex items-center gap-4 text-xs text-muted-foreground">
                <a
                    href="/privacidad"
                    class="hover:text-foreground transition-colors"
                >
                    Privacidad
                </a>
                <a
                    href="/terminos"
                    class="hover:text-foreground transition-colors"
                >
                    Términos
                </a>
            </div>
        </div>
    </div>
</footer>
